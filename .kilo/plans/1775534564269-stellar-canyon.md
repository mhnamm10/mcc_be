# Plan: Optimize `/api/products/{id}/bom` Performance

## Selected Solution: Caching + Pagination

## Implementation Steps

### Step 1: Add Caching for Product Data
Add Spring Cache to reduce repeated DB queries for product data.

**Files to modify:**
- `pom.xml` - Add spring-boot-starter-cache dependency
- `ErpBomApplication.java` - Add @EnableCaching
- `ProductService.java` - Add @Cacheable annotation
- `application.yaml` - Configure cache settings

### Step 2: Add Pagination to BOM Endpoint
Add optional pagination to `GET /api/products/{id}/bom`.

**Files to modify:**
- `BomRowService.java` - Add new method signature
- `BomRowServiceImpl.java` - Implement paginated query
- `BomRowResponse.java` - No changes needed
- `ProductController.java` - Add pagination params

### Step 3: Update GET /bom Endpoint
Modify existing endpoint to support both modes:
- Without params: returns full list (backward compatible)
- With page/size params: returns paginated response

---

## Code Changes Detail

### 1. pom.xml (add cache dependency)
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-cache</artifactId>
</dependency>
```

### 2. ErpBomApplication.java (enable caching)
```java
@SpringBootApplication
@EnableCaching
public class ErpBomApplication { ... }
```

### 3. ProductService.java (add caching)
```java
@Cacheable(value = "products", key = "#id")
public Product getById(UUID id) { ... }
```

### 4. BomRowService.java (add pagination)
```java
Page<BomRowResponse> getBomRowsByProductId(UUID productId, int page, int size);
```

### 5. BomRowServiceImpl.java (implement pagination)
```java
@Override
public Page<BomRowResponse> getBomRowsByProductId(UUID productId, int page, int size) {
    Page<BomRow> pageParam = new Page<>(page, size);
    LambdaQueryWrapper<BomRow> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(BomRow::getProductId, productId).orderByAsc(BomRow::getStt);
    Page<BomRow> result = bomRowMapper.selectPage(pageParam, wrapper);
    return toResponsePage(result);
}
```

### 6. ProductController.java (update endpoint)
```java
@GetMapping("/{id}/bom")
public ResponseEntity<?> getBomRows(
    @PathVariable UUID id,
    @RequestParam(required = false) Integer page,
    @RequestParam(required = false) Integer size) {
    if (page != null && size != null) {
        return ResponseEntity.ok(bomRowService.getBomRowsByProductId(id, page, size));
    }
    return ResponseEntity.ok(bomRowService.getBomRowsByProductId(id));
}
```

---

## Backward Compatibility

- Existing calls without pagination params continue to work
- New pagination params are optional
- Same response format for both modes (List vs Page)

## Estimated Impact

- Caching: ~50-90% reduction in repeated product queries
- Pagination: Significant improvement for BOMs with 100+ rows
